# Spring Boot 开发环境配置
#debug=true

# 系统配置
system.env=dev
# 系统的资源code
system.code=banana

spring.application.name=banana
# Tomcat 配置
server.port=8081
server.tomcat.max-connections=300
server.tomcat.threads.max=100
server.tomcat.connection-timeout=30s

spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# MySQL 数据源配置 (生产环境 - Druid 连接池)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${mysql_url}/file_dev?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=${mysql_username}
spring.datasource.password=${mysql_password}

# MyBatis Plus 配置
# 逻辑删除配置
mybatis-plus.global-config.db-config.logic-delete-field=isDeleted
mybatis-plus.global-config.db-config.logic-delete-value=1
mybatis-plus.global-config.db-config.logic-not-delete-value=0
# 关闭 MyBatis 内置日志，使用自定义拦截器
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.nologging.NoLoggingImpl


# Redis配置
#spring.data.redis.host=wcc.cache01.test.b2c.srv
#spring.data.redis.port=22122
#spring.data.redis.timeout=30000ms
#spring.data.redis.password=cn_mishop_rules_center_test_3K7RZF9iiVhhW
#spring.data.redis.lettuce.pool.min-idle=5
#spring.data.redis.lettuce.pool.max-active=10

# Dubbo 消费者配置（调用 watermelon-service）
dubbo.application.name=banana
dubbo.protocol.name=dubbo
dubbo.registry.address=N/A
dubbo.application.qos-port=33333

# 注册中心会自动添加 application，直连需要手动拼接
dubbo.watermelon.url=dubbo://watermelon.fblue.top:20880?application=watermelon
rpc.secret.watermelon=watermelonSecretDev

# 调用超时时间（毫秒），默认 1000
dubbo.consumer.timeout=60000
